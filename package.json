{
  "name": "@vcsoc/pi-auto-lint",
  "version": "0.2.0",
  "description": "Automatic batch lint, type and format checks for affected projects.",
  "scripts": { "test": "node --test extensions/test/*.test.mjs" },
  "keywords": ["pi-package"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}
